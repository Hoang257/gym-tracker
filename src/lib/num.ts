/**
 * Парсит число из пользовательского ввода. На русской раскладке телефон вводит
 * десятичную запятую, поэтому перед parseFloat заменяем «,» на «.».
 */
export const num = (s: string): number => parseFloat(String(s).replace(',', '.'));
